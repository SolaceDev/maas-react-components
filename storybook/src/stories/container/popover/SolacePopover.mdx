import { Meta, Description, Title, Subtitle, ArgTypes } from "@storybook/blocks";
import * as SolacePopoverStories from "./SolacePopover.stories.tsx";
import { CustomStories } from "../../../../.storybook/CustomStories.tsx";

<Meta of={SolacePopoverStories} />

<Title>Popover</Title>

<Description of={SolacePopoverStories} />

<Subtitle>Code Component Name</Subtitle>
`SolacePopover`

<Subtitle>Component Description</Subtitle>

The SolacePopover component provides a floating container that displays content anchored to a specific element or position on the screen. It's useful for showing supplementary information, contextual actions, or interactive content without navigating away from the current view, allowing users to access additional functionality while maintaining their current context.

The component implements a popover pattern with the purpose of creating non-modal overlays that can display rich content positioned relative to trigger elements. This pattern helps reduce cognitive load by keeping related actions and information spatially connected while providing flexible positioning options for various layout requirements.

<Subtitle>Import Statement</Subtitle>

```tsx
import { SolacePopover } from "@SolaceDev/maas-react-components";
```

<Subtitle>Links</Subtitle>

- **GitHub Source Code**: [SolacePopover.tsx](https://github.com/SolaceDev/maas-react-components/blob/main/src/components/SolacePopover.tsx)
- **GitHub Theme Styles**: <a href="https://github.com/SolaceDev/maas-react-components/blob/main/src/resources/theme.ts#:~:text=MuiPopover" target="_blank">theme.ts (MuiPopover styles)</a>

<Subtitle>Props</Subtitle>

<ArgTypes of={SolacePopoverStories} />

<Subtitle>CSS Classes / Styles</Subtitle>

**Main Component Classes:**

- `.MuiPopover-root` - Root popover container
- `.MuiPopover-paper` - Paper component that contains the popover content
- `.SolacePopover` - Custom class applied to the paper element for Solace-specific styling

**State Classes:**

- `.MuiPopover-root.Mui-open` - Applied when popover is open
- `.MuiPopover-paper` - Paper container with elevation and border styling

**Theme Customization:**

**Color Theming:**
The component uses the theme.palette.ux color system for all color-related styling. Key color theme paths:

<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#FFFFFF",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.background.w100 - Popover background color</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#E0E0E0",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.secondary.w10 - Popover border color</span>
</div>

To customize colors, override the palette.ux values in your theme configuration by extending the base theme and modifying the specific color group and variant you want to change.

**Non-Color Theming:**
Structural and layout customization through component overrides and mixins:

- theme.spacing([NUMBER]) - Padding and margin (e.g., theme.spacing(1) for default padding)
- theme.typography.[VARIANT] - Text styles for popover content
- theme.breakpoints.values.[SIZE] - Responsive behavior breakpoints
- theme.components.MuiPopover.styleOverrides.root - Custom styling for popover container
- theme.components.MuiPopover.styleOverrides.paper - Custom styling for popover paper element

Available mixins for consistent styling:

- theme.mixins.toolbar - Consistent toolbar styling if applicable in popover content

To customize layout and structure, override the component styleOverrides in your theme configuration. You can modify spacing using the theme spacing function, typography using predefined variants, and responsive behavior using breakpoint values.

**Layout Specifications:**

- Anchor reference: Determined by presence of `anchorPosition` prop ("anchorPosition" or "anchorEl")
- Pointer events: Set to "none" when `activateOnHover` is true, "auto" otherwise
- Positioning: Controlled by `anchorOrigin` and `transformOrigin` props
- Margin threshold: Configurable minimum distance from viewport edges
- Content container: Material-UI Paper component with elevation and custom styling
- Default padding: `theme.spacing(1)` (8px)
- Border radius: Inherited from theme (default 4px)
- Transition timeout: `225ms`

<Subtitle>Stories</Subtitle>

<CustomStories />
