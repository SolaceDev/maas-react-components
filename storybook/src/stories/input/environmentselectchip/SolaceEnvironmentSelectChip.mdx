import { Meta, Description, Title, Subtitle, ArgTypes } from "@storybook/blocks";
import * as SolaceEnvironmentSelectChipStories from "./SolaceEnvironmentSelectChip.stories.tsx";
import { CustomStories } from "../../../../.storybook/CustomStories.tsx";

<Meta of={SolaceEnvironmentSelectChipStories} />

<Title>Environment Select Chip</Title>

<Description of={SolaceEnvironmentSelectChipStories} />

<Subtitle>Code Component Name</Subtitle>
`SolaceEnvironmentSelectChip`

<Subtitle>Component Description</Subtitle>

The Solace Environment Select Chip component provides a dropdown selection interface for choosing environments with visual color coding and icons, useful for environment selection in applications where users need to switch between different environments like development, staging, and production and allowing users to easily identify and select environments through distinctive visual cues. The component implements a custom select pattern with the purpose of making environment selection intuitive and visually distinctive, helping users quickly identify different environments through color coding and iconography while providing a compact dropdown interface for space-efficient layouts.

<Subtitle>Import Statement</Subtitle>

```tsx
import { SolaceEnvironmentSelectChip } from "@SolaceDev/maas-react-components";
```

<Subtitle>Links</Subtitle>

- **GitHub Source Code**: [SolaceEnvironmentSelectChip.tsx](https://github.com/SolaceDev/maas-react-components/blob/main/src/components/SolaceEnvironmentSelectChip.tsx)
- **GitHub Theme Styles**: <a href="https://github.com/SolaceDev/maas-react-components/blob/main/src/resources/theme.ts#:~:text=MuiSelect" target="_blank">theme.ts (MuiSelect styles)</a>

<Subtitle>Props</Subtitle>

<ArgTypes of={SolaceEnvironmentSelectChipStories} />

<Subtitle>CSS Classes / Styles</Subtitle>

**Main Component Classes:**

- Custom styled components based on MUI Base Select
- `ColorLayer` - Colored background and border styling for chip appearance
- `StateLayer` - Hover and interaction state styling
- `ContentLayer` - Main button content with dropdown indicator
- `ContainerLayer` - Root select container with focus and expanded states

**MUI Base Select Classes:**

- `.MuiSelect-root` - Root select element
- `.MuiSelect-expanded` - Applied when dropdown is open
- `.MuiSelect-focusVisible` - Applied when select receives keyboard focus

**Option Classes:**

- `.MuiOption-root` - Individual option styling
- `.MuiOption-selected` - Applied to the currently selected option
- `.MuiOption-highlighted` - Applied when option is highlighted/hovered
- `.MuiOption-disabled` - Applied to disabled options

**Custom Classes:**

- `.state-layer` - Hover state styling for the chip button
- `IconWrapper` - Colored icon container within options
- `StickyChildren` - Bottom-positioned sticky content area

**Theme Customization:**

**Color Theming:**
The component uses the theme.palette color system for all color-related styling. Key color theme paths:

<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#FFFFFF",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.background.w10 - Dropdown background color</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#E6FAF4",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.brand.w10 - Selected/highlighted option background</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "rgba(3, 34, 59, 0.1)",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.stateLayer.w10 - Hover state background</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "rgba(3, 34, 59, 0.2)",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.stateLayer.w20 - Expanded state background</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#000000",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.secondary.w100 - Border color for white backgrounds</span>
</div>

To customize colors, override the palette values in your theme configuration by extending the base theme and modifying the specific color group and variant you want to change.

**Non-Color Theming:**
Structural and layout customization through component overrides and mixins:

- theme.typography.body1 - Text styling for chip and options
- theme.spacing(0.5) - Border radius and icon padding
- theme.spacing(1, 2) - Option padding
- theme.spacing(2) - Gap between elements
- theme.mixins.chip.base - Standard chip styling patterns

**Layout Specifications:**

- Chip border radius: `4px` (theme.spacing(0.5))
- Icon padding: `4px` (theme.spacing(0.5))
- Option padding: `8px 16px` (theme.spacing(1, 2))
- Gap between elements: `16px` (theme.spacing(2))
- Dropdown max height: `min(416px, calc(100vh - 80px))` (theme.spacing(52, 10))
- Dropdown min height: `80px` (theme.spacing(10))
- Dropdown min width: `320px` (theme.spacing(40))
- Text max width: `134px` with ellipsis overflow
- Chip height: `32px` (theme.spacing(4))
- Transition duration: `200ms` for hover and focus effects

<Subtitle>Stories</Subtitle>

<CustomStories />
