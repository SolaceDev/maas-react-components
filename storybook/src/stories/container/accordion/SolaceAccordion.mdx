import { Meta, Description, Title, Subtitle, ArgTypes } from "@storybook/blocks";
import * as SolaceAccordionStories from "./SolaceAccordion.stories.tsx";
import { CustomStories } from "../../../../.storybook/CustomStories.tsx";

<Meta of={SolaceAccordionStories} />

<Title>SolaceAccordion</Title>

<Description of={SolaceAccordionStories} />

<Subtitle>Code Component Name</Subtitle>
`SolaceAccordion`

<Subtitle>Component Description</Subtitle>

The SolaceAccordion component provides an expandable/collapsible container that helps organize content into sections. It's useful for reducing visual clutter by hiding content until needed, allowing users to focus on specific information.

The component implements a progressive disclosure pattern with the purpose of only revealing essential information up front, and moving secondary/complex content out of the main interface into an expandable area. This pattern keeps users on track of the main task while still making additional information available.

<Subtitle>When To Use It</Subtitle>

Use accordions when you have multiple sections of related content that don't need to be visible simultaneously. This interaction pattern is particularly effective for organizing large amounts of content without overwhelming the user interface.

Common use cases include FAQ sections, settings panels with advanced options, table row details, list item expansions, and card-based layouts where additional information needs to be accessible but not always visible.

<Subtitle>When Not To Use It</Subtitle>

- The content is critical and should always be visible to users
- There are only a few short sections that could easily fit on the screen at once
- Users need to compare information across multiple sections simultaneously
- The accordion would create an unnecessary extra step for frequently accessed content
- The content has a natural sequential flow that users should follow step by step
- The collapsed state might hide important information that users need to see immediately

<Subtitle>Import Statement</Subtitle>

```tsx
import { SolaceAccordion } from "@SolaceDev/maas-react-components";
```

<Subtitle>Links</Subtitle>

- **GitHub Source Code**: [SolaceAccordion.tsx](https://github.com/SolaceDev/maas-react-components/blob/main/src/components/SolaceAccordion.tsx)
- **GitHub Theme Styles**: <a href="https://github.com/SolaceDev/maas-react-components/blob/main/src/resources/theme.ts#:~:text=MuiAccordion" target="_blank">theme.ts (MuiAccordion, MuiAccordionSummary, MuiAccordionDetails styles)</a>

<Subtitle>Props</Subtitle>

<ArgTypes of={SolaceAccordionStories} />

<Subtitle>CSS Classes / Styles</Subtitle>

**Main Component Classes:**

- `.MuiAccordion-root` - Root accordion container
- `.MuiAccordionSummary-root` - Summary/header section
- `.MuiAccordionDetails-root` - Expandable details section

**State Classes:**

- `.Mui-expanded` - Applied when accordion is expanded
- `.Mui-disabled` - Applied when accordion is disabled
- `.hasHoverEffect` - Applied when hover prop is true

**Indicator Classes:**

- `.indicator-info` - Info variant indicator styling
- `.indicator-error` - Error variant indicator styling
- `.indicator-warn` - Warning variant indicator styling
- `.indicator-success` - Success variant indicator styling
- `.indicator-secondary` - Secondary variant indicator styling

**Theme Customization:**

**Color Theming:**
The component uses the theme.palette.ux color system for all color-related styling. Key color theme paths:

<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#E0E0E0",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.secondary.w10 - Default border and divider color for accordion sections</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#FF4D4D",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.error.w100 - Error state indicator color</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#2196F3",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.info.w100 - Info state indicator color</span>
</div>

To customize colors, override the palette.ux values in your theme configuration by extending the base theme and modifying the specific color group and variant you want to change.

**Non-Color Theming:**
Structural and layout customization through component overrides and mixins:

- theme.spacing([NUMBER]) - Padding between elements (e.g., theme.spacing(2) for 16px)
- theme.typography.body1 - Text style for accordion content
- theme.typography.subtitle1 - Text style for accordion headers
- theme.breakpoints.values.[SIZE] - Responsive behavior breakpoints
- theme.components.MuiAccordion.styleOverrides.root - Custom styling for accordion container
- theme.components.MuiAccordionSummary.styleOverrides.root - Custom styling for header section
- theme.components.MuiAccordionDetails.styleOverrides.root - Custom styling for content section

Available mixins for consistent styling:

- theme.mixins.toolbar - Consistent toolbar styling if applicable

To customize layout and structure, override the component styleOverrides in your theme configuration. You can modify spacing using the theme spacing function, typography using predefined variants, and responsive behavior using breakpoint values.

**Layout Specifications:**

- Summary section padding: `theme.spacing(0.75, 0, 0.75, 1)` (6px 0px 6px 8px)
- Details section padding: `theme.spacing(1, 2, 2, 6)` (8px 16px 16px 48px)
- Expand icon: 24x24px ArrowRight icon
- Transition timeout: `350ms`

<Subtitle>Stories</Subtitle>

<CustomStories />
