import { Meta, Description, Title, Subtitle, ArgTypes } from "@storybook/blocks";
import * as SolaceGridListStories from "./SolaceGridList.stories.tsx";
import { CustomStories } from "../../../../.storybook/CustomStories.tsx";

<Meta of={SolaceGridListStories} />

<Title>Solace Grid List</Title>

<Description of={SolaceGridListStories} />

<Subtitle>Code Component Name</Subtitle>
`SolaceGridList`

<Subtitle>Component Description</Subtitle>

The Solace Grid List component provides a flexible way to display collections of data in a structured grid layout. It's useful for rendering tabular data with customizable columns and rows, allowing users to view and interact with large datasets efficiently.

The component implements a grid-based layout pattern with the purpose of organizing data into visually structured rows and columns. This pattern helps users scan and compare information quickly while still supporting advanced features like virtualization for performance optimization.

<Subtitle>Usage</Subtitle>

- **Organized Collections**: Use to display a collection of items (typically images or cards) in an organized grid format.
- **Visual Comprehension**: Helps improve the visual understanding and scannability of the content presented.
- **Uniform Items**: Standard grid lists are ideal when all items have equal importance, maintaining consistent container size, aspect ratio, and spacing.
- **Hierarchical Display**: For emphasizing certain items, consider a quilted layout (if supported, or by customizing item sizes) to create visual hierarchy through varied container dimensions.

<Subtitle>Import Statement</Subtitle>

```tsx
import { SolaceGridList } from "@SolaceDev/maas-react-components";
```

<Subtitle>Links</Subtitle>

- **GitHub Source Code**: [SolaceGridList.tsx](https://github.com/SolaceDev/maas-react-components/blob/main/src/components/layout/SolaceGridList.tsx)
- **GitHub Theme Styles**: <a href="https://github.com/SolaceDev/maas-react-components/blob/main/src/resources/theme.ts#:~:text=layoutComponent_ImageList" target="_blank">theme.ts (layoutComponent_ImageList styles)</a>

<Subtitle>Props</Subtitle>

<ArgTypes of={SolaceGridListStories} />

<Subtitle>CSS Classes / Styles</Subtitle>

**Main Component Classes:**

- `.layoutComponent_ImageList-row` - Individual grid row container
- `.layoutComponent_ImageList-virtualRow` - Virtual row container for virtualized lists
- `.layoutComponent_ImageList-list` - Main list container
- `.layoutComponent_ImageList-border` - Border wrapper around the list

**State Classes:**

- `.selected` - Applied when a row is selected
- `.headerRow` - Applied to the header row container
- `.indicator-info` - Applied when indicatorVariant is "info"
- `.indicator-error` - Applied when indicatorVariant is "error"
- `.indicator-warn` - Applied when indicatorVariant is "warn"
- `.indicator-success` - Applied when indicatorVariant is "success"
- `.indicator-secondary` - Applied when indicatorVariant is "secondary"

**Emphasis Classes:**

- `.emphasized` - Applied when a row should be emphasized with special styling

**Theme Customization:**

**Color Theming:**
The component uses the theme.palette.ux color system for all color-related styling. Key color theme paths:

<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#000000",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.primary.text.wMain - Primary text color for content</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#757575",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.secondary.text.wMain - Secondary text color for headers</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#F5F5F5",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.background.w10 - Default background color</span>
</div>

To customize colors, override the palette.ux values in your theme configuration by extending the base theme and modifying the specific color group and variant you want to change.

**Non-Color Theming:**
Structural and layout customization through component overrides and mixins:

- theme.spacing([NUMBER]) - Padding and margin (e.g., theme.spacing(1) for default padding)
- theme.typography.[VARIANT] - Text styles for grid list content
- theme.breakpoints.values.[SIZE] - Responsive behavior breakpoints
- theme.components.MuiImageList.styleOverrides.root - Custom styling for grid list container
- theme.components.MuiImageListItem.styleOverrides.root - Custom styling for grid list items

Available mixins for consistent styling:

- theme.mixins.toolbar - Consistent toolbar styling if applicable in grid list headers

To customize layout and structure, override the component styleOverrides in your theme configuration. You can modify spacing using the theme spacing function, typography using predefined variants, and responsive behavior using breakpoint values.

**Layout Specifications:**

- Grid template columns: Defined by the `gridTemplate` prop using CSS Grid syntax
- Item height: Calculated dynamically using `getGridListItemHeight()` utility
- Virtual list height: Configurable through `virtualizedListOption.height`
- Scroll mask: Applied when `numOfGridListItemDisplayed` is set to create fade effect
- Transition timeout: `200ms`

<Subtitle>Stories</Subtitle>

<CustomStories />
