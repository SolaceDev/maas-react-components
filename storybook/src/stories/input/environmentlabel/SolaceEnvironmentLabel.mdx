import { Meta, Description, Title, Subtitle, ArgTypes } from "@storybook/blocks";
import * as SolaceEnvironmentLabelStories from "./SolaceEnvironmentLabel.stories.tsx";
import { CustomStories } from "../../../../.storybook/CustomStories.tsx";

<Meta of={SolaceEnvironmentLabelStories} />

<Title>Solace Environment Label</Title>

<Description of={SolaceEnvironmentLabelStories} />

<Subtitle>Code Component Name</Subtitle>
`SolaceEnvironmentLabel`

<Subtitle>Component Description</Subtitle>

The Solace Environment Label component provides a visual identifier for environments with colored icons and text labels. It's useful for displaying environment information in a compact, easily recognizable format, allowing users to quickly identify different environments by their distinctive colors and icons.

The component implements a horizontal layout pattern with the purpose of showing environment context at a glance. This pattern helps users distinguish between different environments while maintaining visual consistency across the application.

<Subtitle>When To Use It</Subtitle>

Use Solace Environment Label when you need to display environment information in a compact, visual format. This display pattern is particularly effective for environment identification and context-aware interfaces.

Common use cases include:

- Dropdown menu items showing available environments with visual distinction
- Environment selection interfaces where users need to choose between multiple environments
- Dashboard headers or status bars displaying current environment context
- Form fields requiring environment selection with clear visual identification
- Environment listings where users need to quickly scan and identify different environments

<Subtitle>When Not To Use It</Subtitle>

- For actionable environment switching - use SolaceEnvironmentChip with click handlers instead
- When environments need to be removable - use SolaceEnvironmentChip with onDelete functionality
- For very long environment names in constrained spaces - consider using tooltips with shortened labels
- When you need interactive behaviors like hover states - use button or chip components instead
- For non-environment categorization - use regular labels or tags for other types of content
- When color coding is not important - use simple text labels for better accessibility

<Subtitle>Import Statement</Subtitle>

```tsx
import { SolaceEnvironmentLabel } from "@SolaceDev/maas-react-components";
```

<Subtitle>Links</Subtitle>

- **GitHub Source Code**: [SolaceEnvironmentLabel.tsx](https://github.com/SolaceDev/maas-react-components/blob/main/src/components/SolaceEnvironmentLabel.tsx)
- **GitHub Theme Styles**: <a href="https://github.com/SolaceDev/maas-react-components/blob/main/src/resources/theme.ts#:~:text=typography" target="_blank">theme.ts (typography styles)</a>

<Subtitle>Props</Subtitle>

<ArgTypes of={SolaceEnvironmentLabelStories} />

<Subtitle>CSS Classes / Styles</Subtitle>

**Main Component Classes:**

- Custom styled components (no standard MUI classes)
- `Container` - Root label container with inline-flex layout
- `ColoredIcon` - Icon container with colored background
- `Label` - Text label with overflow handling

**Variant Classes:**

- `variant="standard"` - Standard label styling with body1 typography
- `variant="title"` - Title styling with h1 typography for prominence

**Theme Customization:**

**Color Theming:**
The component uses the theme.palette.ux color system for all color-related styling. Key color theme paths:

<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#000000",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.ux.primary.text.wMain - Label text color</span>
</div>
<div style={{ display: "flex", alignItems: "center", margin: "8px 0" }}>
	<div
		style={{
			width: "16px",
			height: "16px",
			backgroundColor: "#FFFFFF",
			border: "1px solid rgba(0,0,0,0.2)",
			borderRadius: "3px",
			marginRight: "8px"
		}}
	></div>
	<span>theme.palette.common.white - Icon border color for white backgrounds</span>
</div>

To customize colors, override the palette.ux values in your theme configuration by extending the base theme and modifying the specific color group and variant you want to change.

**Non-Color Theming:**
Structural and layout customization through component overrides and mixins:

- theme.typography.body1 - Standard variant typography
- theme.typography.h1 - Title variant typography
- theme.spacing(3) - Component height and icon minimum width
- theme.spacing(2) - Gap between icon and label
- theme.spacing(0.5) - Icon border radius

**Layout Specifications:**

- Container height: `24px` (theme.spacing(3))
- Icon minimum width: `24px` (theme.spacing(3))
- Gap between elements: `16px` (theme.spacing(2))
- Icon border radius: `4px` (theme.spacing(0.5))
- Text overflow: `ellipsis` with `white-space: nowrap`
- Icon border: `1px solid` (transparent unless white background)
- Container layout: `inline-flex` with `align-items: center`
- Label max-width: `calc(100% - 40px)` to accommodate icon and spacing

<Subtitle>Stories</Subtitle>

<CustomStories />
